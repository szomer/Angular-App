const imageBaseUrl = 'https://image.tmdb.org/t/p';

// Stores the image URL for different sizes
export const IMAGE_SIZES = {
  small: `${imageBaseUrl}/w185`,
  medium: `${imageBaseUrl}/w342`,
  large: `${imageBaseUrl}/original`,
};
